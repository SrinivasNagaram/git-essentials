// Git Commends 
$ git clone		[repository url] // git@github.com:SrinivasNagaram/git-essentials.git
$ git init      // Initialize Git 
$ git pull		// To download other changes 
$ git status	// Check changes in local
$ git add		[filename]  // to add your local changes into git [-A for all]
$ git add		. Entire directory 
$ git commit	-m "Message"
$ git push		origin master [branch name]


//Generate SSH key
$ ssh-keygen
$ cat ~/.ssh/id_rsa.pub //Copy Public Key